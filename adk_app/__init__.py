"""ADK application wrappers for WorkflowGenie.

This package provides ADK-style agents, tools and a workflow orchestrator
that wrap the existing project logic so you can run locally (via the shim)
and deploy to Agent Engine when using the real ADK package.
"""
